node_modules/
dist/
dist-electron/
release/
*.log

# Datos locales del usuario — nunca se suben (catálogo, imágenes de producto,
# portada/header/footer de marca, base de datos). La app los recrea/regenera
# en tiempo de ejecución; ver README para el flujo de importación.
data/

# PDFs de catálogos de proveedor sueltos en la raíz (archivos de prueba/reales
# del usuario, no parte del código fuente).
/*.pdf

# Prisma
packages/infrastructure/prisma/dev.db*

# Copia del navegador de Playwright generada por
# apps/desktop/scripts/copy-playwright-browser.mjs antes de empaquetar
# (ver apps/desktop/electron-builder.yml) — no es código fuente, se
# regenera desde el caché local de Playwright en cada build.
apps/desktop/build/ms-playwright/

.env
**/.env
.DS_Store
